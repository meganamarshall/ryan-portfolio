import styled from 'styled-components';

const LinksStyled = styled.div `
  ul {
    margin-top: 10vh;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: center;

  }

  li {
  }
`;

export default LinksStyled;
